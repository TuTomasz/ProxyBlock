
Create Virtual environment.

~virtualenv -p python3 <desired-path>

cd into ml_model folder and run 

~pip install -r requirements.txt

return to ProxyBlock main directory 

~run npm install

Once all modules are installed run

~node index.js